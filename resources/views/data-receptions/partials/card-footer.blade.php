<div class="card-footer">
	<a href="{{ route('data-receptions.show', $device->id) }}" class="btn btn-sm btn-primary m-1">----</a>
	<a href="{{ route('data-receptions.show', $device->id) }}" class="btn btn-sm btn-primary m-1">----</a>
	<a href="{{ route('data-receptions.show', $device->id) }}" class="btn btn-sm btn-primary m-1">----</a>
	<a href="{{ route('data-receptions.show', $device->id) }}" class="btn btn-sm btn-primary m-1">----</a>
</div>